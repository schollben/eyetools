


##########################
Monocular (all events, combine eyes):

MAIN sequence (examine per animal)
  a. amplitude vs peak velocity
  b. amplitude vs duration
  c. amplitude vs peak velocity * duration
  *** amplitude == distance between 2 furtherest points on trajectory


VOR characteristics
  a. head pitch vs LE/RE horizontal/vertical position
  b. head ROLL vs LE/RE horizontal/vertical position
  c. head velocity (total ang or horizontal) vs LE/RE velocity (should see VOR and gaze shifts)
  d. head velocity vs eye velocity OUTSIDE of gaze/eye shifts
  e. horizontal eye velocity VS running speed (scatter or distributions)
  stationary vs running

Following the organization and structure of main_sequence.py build a cell-based script to examine VOR characteristics. 
Data should be able to be examined on a an animal by animal basis and then pooled across animals for different age groups: 
pool_by_eo = True  # False: one panel per session | True: one panel per EO range
eo_bins = [(0, 4), (5, 9), (10, 20)]  # early / middle / late, inclusive
Note: we must use periods of times OUTSIDE of saccades. We can decide what the window should be, but the saccade extraction helper function 
should provide onset and peak time for each saccade event.
Note: to combine data across eyes we may need to flip one eye's horizontal axis (sign flip)
Here are the components to examine (best to be examined and plotted in separate cells):
1. head total angular velocity compared to eye velocity as a scatter plot. plot a single session and plot all data together (2 separate plots). 
if pool_by_eo=True plot all data binned by eo. these plots can contain saccade events (as compared to next section).
2. same as (1) but OUTSIDE of saccade events
3. head pitch vs eye vertical position (combine data from both eyes)
4. head yaw vs eye horizontal position (combine data from both eyes)
5. build out similar plots for stationary vs running. set a threshold for each and then regenerate plots. How else can we evaluate the relationship between VOR and running?
6. examine distributions of head total angular velocity over time. this can only be done with pool_by_eo = True. goal to examine mean and variance as a measure of head stability.
plots should consider stationary and running periods. plot head total angular velocity over time vs running speed for running periods (individual defined session and collapsed across binned sessions)
What other analyses should i consider here? Ask questions.


INTEGRATOR ('saccade free periods')
  a. eye position vs velocity (X/Y or combine?)
  b. biases or shifts over time for animals?

Following the organization and structure of main_sequence.py build a cell-based script to INTEGRATOR ('saccade free periods') characteristics. 
Data should be able to be examined on a an animal by animal basis and then pooled across animals for different age groups: 
pool_by_eo = True  # False: one panel per session | True: one panel per EO range
eo_bins = [(0, 4), (5, 9), (10, 20)]  # early / middle / late, inclusive
Note: we must use periods of times OUTSIDE of saccades. We can decide what the window should be, but the saccade extraction helper function 
should provide onset and peak time for each saccade event.
Note: to combine data across eyes we may need to flip one eye's horizontal axis (sign flip)
1. LE and RE position compared to velocity. Separate plots for now. 2 plots for a defined session. additional plots with binned data dependeing on 
if pool_by_eo=True.  if pool_by_eo=True, plot all data binned by eo (3 age groups = 3 plots)
2. examine eye position vs velocity on an animal-by-animal basis over time.
the challenge here is to be careful how we define "saccade-free" periods. this may take some tweaking of defined thresholds. 
What other analyses should i consider here? Ask questions.



eye movement statistics (eye-in-head and eye+head)
  a. mean saccade angular speed profiles binned by net amplitude
  b. mean angular displacement profiles binned by net amplitude
*** need to compare peak velocity timepoints for saccades? or can we examine continuous data?





Following the organization and structure of main_sequence.py build a cell-based script to examine EYE-HEAD DYNAMICS
Data should be able to be examined on a an animal by animal basis and then pooled across animals for different age groups: 
pool_by_eo = True  # False: one panel per session | True: one panel per EO range
eo_bins = [(0, 4), (5, 9), (10, 20)]  # early / middle / late, inclusive
Here are different analyses and plots to generate. They are likely not organized well and could use reorganization.
Lets discuss each and build an depth plan to cover all potential, interesting analyses, plots, and characteristics to quantify.
Note: some of these are repeating and overlapping. This is a rough list to begin devleoping a plan.
Note 2: we should only examine combined eye (LE and RE) statistics or an individual eye (LE or RE). 
- head saccade onset compared to during rotation? compare acceleration and velocity?
- head-rotation (yaw) triggered saccade average waveform (collapsed across eyes- combine left and right eye). 
- examine timing of head rotations (yaw) and eye movements to characterize 
- head VS eye -> which came first? categorize types? saccades different?

** counter-rotations (like in the 2026 current biology Jason Kerr paper) **
-  peak velocity vs peak negative velocity of eye movements during head rotation - compensation
- peak velocity vs peak positive velocity of eye movements during head rotation
-  consider head saccade onset vs during head rotation?  

what about examining timing of head rotations (yaw) and eye movements? which cell does this?
in addition, what about categorizing the different head-eye relationships? which cell does this?



##########################

Following the organization and structure of main_sequence.py build a cell-based script to examine BINOCULARITY
Data should be able to be examined on a an animal by animal basis and then pooled across animals for different age groups: 
pool_by_eo = True  # False: one panel per session | True: one panel per EO range
eo_bins = [(0, 4), (5, 9), (10, 20)]  # early / middle / late, inclusive
Each section/number below should be a separate cell. 
As much as possible, keep cells contained and without defined helper functions.

1. correlation in position and velocity between the two eyes during saccades
  1a. LE vs RE horizontal position 
  1b. Log-scaled joint distribution of horizontal saccade velocity
  1c. Log-scaled joint distribution of horizontal saccade velocity AND angular head velocity (or horizontal?)
  1d. Cross correlation of horizontal eye position across the two eyes (purely for exploratin of data, dont have a specific hypothesis)

2. correlation in position and velocity between the two eyes OUTSIDE of saccades (this would likely include fixations and smooth pursuit)
- flag for locomotion (stationary vs running)

3. interocular peak velocity coupling. 
- use 2D binnning?
- examine joint distribution of LE and RE amplitude/velocity (example: LE is x-axis, RE is y -axis)
- need to condsider focusing only on horizontal/vertical movements OR total angular displacement/position/velocity (combining X and Y).
perhaps this should be a flag throughout.

4. eye end-point density for monocular/binocular events
  Note: this section not well thought out

5. binocular stability during "fixation" periods
  6a. compute LE - RE velocity in between gaze/eye shifts during head movements
  6b. keep track of CW and CCW turns
  6b. also consider outside head rotations (head pitch and roll magnitude < 10 deg)?

6. vergence = LE - RE position (deg)
  7a. propotion of time over development
  7b. examine pitch vs vergence (compensation)
  Note: this section not well thought out

##########################

* locomotion notes--
head linear speed (running or not)
head angular speed


##########################
Arousal: 

define active and passive states based on head speed and/or pupil size
probability density distribution pupil size during 'active'
relationship between running, head rotations, pupil size (how to define states)

* notes--
z-scored pupil size? why are eyes not same on same animals?

